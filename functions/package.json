{
  "name": "functions",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions --config ../infra/firebase.json --project neohydraulik"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.9.0",
    "seedrandom": "^3.0.5"
  },
  "devDependencies": { "typescript": "^5.8.3" }
}
